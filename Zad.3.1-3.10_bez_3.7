# Zadanie 3.1

x = 2; y = 3;
if (x > y):
    result = x;
else:
    result = y; # Do tego momentu jest poprawnie

# for i in "axby": if ord(i) < 100: print (i) # Od tej linijki zaczyna się błąd, terminal pokazuje SyntaxError. Petla for i warunek if muszą być w innych wierszach
# Zamiast tego:

for i in "axby": 
    if ord(i) < 100: print (i)
for i in "axby": print (ord(i) if ord(i) < 100 else i) # W ostatniej linijce nie ma instrukcji warunkowej i pętli w jednym wierszu, kod działą poprawnie

# Zadanie 3.2
# L = [3, 5, 4] ; L = L.sort()   
# Poprawnie:
L = [3, 5, 4]
L.sort()
print (L)
# x, y = 1, 2, 3      # Są dwie zmienne x i y, ale 3 wartości. Python "nie wie" jak je rozdzielić
# Poprawnie, to np:
x, y, z = 1, 2, 3
print (x, y, z)

#X = 1, 2, 3 ; X[1] = 4     # X jest krotką. Próba wprowadzenia zmian do krotki kończy się błędem
# Poprawnie to np:
X = [1, 2, 3] ; X[1] = 4
print (X[1]) 

# X = [1, 2, 3] ; X[3] = 4  # X[3] nie istnieje, bo listy są indeksowane od 0
# Poprawnie to np:
X.append(4)
print (X)
# X = "abc" ; X.append("d")  # Stringi w pythonie są niemodyfikowalne, dlatego funkcja append nie zadziała.
# Poprawnie to np:
X = ["abc"] ; X.append("d")
print (X)

# L = list(map(pow, range(8)))  # 
L = list(map(lambda x: pow(x, 2), range(8)))
print (L)
# lub prościej:
L = [x**2 for x in range(8)]
print (L)

# Zadanie 3.3
for i in range(31):
    if i % 3 == 0:
        continue
    print (i)

n = 0
while n < 31:
    if n % 3 != 0:
        print (n)
    n += 1

# Zadanie 3.4
while True:
    x = input("Podaj x , lub wpisz stop: ")
    if x == "stop":
        print("Wyłączam program")
        break
    try:
        x = float(x)
        print (x**3)
    except ValueError:
        print("Podałeś x, który nie jest typu float")
        continue

# Zadanie 3.5
while True:
    x = input("Podaj liczbę, lub wpisz stop: ")
    if x == "stop":
        print("Wyłączam program")
        break
    try:
        góra = "|"

        x = int(x)
        for i in range(x):
            góra += "....|"

        dół = str(0)

        for i in range(1, x + 1):
            dół += str(i).rjust(5)
        
        print(góra)
        print(dół)



    except ValueError:
        print("Podałeś liczbę, który nie jest typu int")
        continue


# Zadanie 3.6
while True:
    b = input("Podaj długość b: ")
    a = input("Podaj wysokość a: ")

    try:
        a = int(a)
        b = int(b)


        długość = "+"
        for i in range (b):
            długość += "---+"
               
        przedział = "|"
        for i in range (b):
            przedział += "   |"

        for i in range (a):
            print(długość)
            print(przedział)
            
        print (długość)
        
        
    except ValueError:
        print("Podałeś liczbę, który nie jest typu int")
        continue





        




